using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RevendicationTuile : MonoBehaviour
{
    public TuileManager tuile;
    public List<Revendication> revendicateurs = new List<Revendication>();


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
